cmake_minimum_required(VERSION 3.25)

project(crude_engine)

find_package(Vulkan REQUIRED)

file(GLOB_RECURSE SRC *.cc)
file(GLOB_RECURSE HEADERS *.h *.inl)

set(CRUDE_SOURCES ${SRC} ${HEADERS})

add_library(${PROJECT_NAME} SHARED ${CRUDE_SOURCES})

target_include_directories(${PROJECT_NAME} PUBLIC ${Vulkan_INCLUDE_DIR} ${CMAKE_CURRENT_SOURCE_DIR} ${JoltPhysics_SOURCE_DIR}/..)
target_link_libraries(${PROJECT_NAME} PUBLIC stb flecs tlsf SDL3-shared ${Vulkan_LIBRARY} vma cgltf spirv_reflect miniaudio enkiTS TracyClient cjson_cmake imguizmo_cmake meshoptimizer renderdoc_api_cmake Jolt nfd)

source_group(core REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/core/*")
source_group(platform REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/platform/*")
source_group(graphics REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/graphics/*")
source_group(graphics/passes REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/graphics/passes/*")
source_group(scene REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/scene/*")
source_group(physics REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/physics/*")
source_group(sound REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/sound/*")
source_group(gui REGULAR_EXPRESSION "${CMAKE_CURRENT_SOURCE_DIR}/gui/*")
source_group(engine FILES engine.cc engine.h)

if(NOT CMAKE_BUILD_TYPE)
set(CMAKE_BUILD_TYPE Release)
endif()

set(CMAKE_CXX_FLAGS_RELEASE "-O3")

target_compile_definitions(${PROJECT_NAME} PUBLIC 
  ## !MAIN
  CRUDE_GRAPHICS_OPTIMAIZE_SHADERS=1
  CRUDE_GRAPHICS_VALIDATION_LAYERS_ENABLED=1
  #CRUDE_PRODUCTION=0
  #CRUDE_GRAPHICS_RAY_TRACING_ENABLED=0
  #CRUDE_GRAPHICS_FRAME_DESCRIPTOR_SETS=0

  ## !CORE
  CRUDE_CORE_HASHMAP_INITIAL_CAPACITY=16
  CRUDE_CORE_PROCESS_LOGGER_BUFFER_SIZE=256
  CRUDE_CORE_PROCESS_OUTPUT_BUFFER_SIZE=1024

  ## !GRAPHICS
  CRUDE_GRAPHICS_CONSTANT_MESHLET_MAX_VERTICES=128
  CRUDE_GRAPHICS_CONSTANT_MESHLET_MAX_TRIANGLES=124
  CRUDE_GRAPHICS_CONSTANT_MESHLET_CONE_WEIGHT=0.0
  CRUDE_GRAPHICS_TEXTURE_UPDATE_COMMANDS_THREAD_ID=1
  CRUDE_GRAPHICS_ASYNCHRONOUS_LOADER_UPLOAD_REQUESTS_LIMIT=1024
  CRUDE_GRAPHICS_ASYNCHRONOUS_LOADER_FILE_LOAD_REQUESTS_LIMIT=1024
  CRUDE_GRAPHICS_FRAME_DESCRIPTOR_POOL_ELEMENTES_COUNT=1024
  CRUDE_GRAPHICS_DEPTH_AND_STENCIL_CLEAR_COLOR_INDEX=8
  CRUDE_GRAPHICS_SWAPCHAIN_BARRIER_COMMAND_BUFFER_THREAD_ID=0
  CRUDE_GRAPHICS_TEXTURE_COPY_FROM_BUFFER_THREAD_ID=0  
  CRUDE_GRAPHICS_MAX_SWAPCHAIN_IMAGES=3
  CRUDE_GRAPHICS_MAX_IMAGE_OUTPUTS=8
  CRUDE_GRAPHICS_MAX_DESCRIPTOR_SET_LAYOUTS=8
  CRUDE_GRAPHICS_MAX_SHADER_STAGES=5      
  CRUDE_GRAPHICS_MAX_DESCRIPTORS_PER_SET=64
  CRUDE_GRAPHICS_UBO_ALIGNMENT=256
  CRUDE_GRAPHICS_MAX_SET_COUNT=32
  CRUDE_GRAPHICS_MAX_BINDLESS_RESOURCES=1024
  CRUDE_GRAPHICS_BINDLESS_DESCRIPTOR_SET_INDEX=0
  CRUDE_GRAPHICS_MATERIAL_DESCRIPTOR_SET_INDEX=1
  CRUDE_GRAPHICS_BINDLESS_TEXTURE_BINDING=10
  CRUDE_GRAPHICS_BINDLESS_IMAGE_BINDING=11
  CRUDE_GRAPHICS_DEPTH_PYRAMID_PASS_MAX_LEVELS=16
  CRUDE_GRAPHICS_TETRAHEDRON_SHADOWMAP_SIZE=8192
  CRUDE_GRAPHICS_RENDER_GRAPH_MAX_RESOURCES_COUNT=1024
  CRUDE_GRAPHICS_RENDER_GRAPH_MAX_NODES_COUNT=1024
  CRUDE_GRAPHICS_SCENE_RENDERER_DEBUG_LINE_2D_OFFSET=1000
  CRUDE_GRAPHICS_SCENE_RENDERER_MAX_DEBUG_LINES=640000
  CRUDE_GRAPHICS_SCENE_RENDERER_MAX_DEBUG_CUBES=60000
  CRUDE_GRAPHICS_SCENE_RENDERER_LIGHT_Z_BINS=16
  CRUDE_GRAPHICS_SCENE_RENDERER_LIGHTS_MAX_COUNT=256
  CRUDE_GRAPHICS_SCENE_RENDERER_LIGHT_TILE_SIZE=8
  CRUDE_GRAPHICS_SCENE_RENDERER_MAX_MESHLETS_PER_LIGHT=45000
)

if(MSVC)
  target_compile_options(${PROJECT_NAME} PRIVATE "/MP" "/ZI")
endif()
